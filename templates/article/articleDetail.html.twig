{% extends 'base.html.twig' %} {% block title %}Hello ArticleController!{%
endblock %} {% block body %}
<div class="m-10">
    <a class="text-2xl" href="/article">⬅</a>
  <h1 class="text-4xl my-5">{{ article.titre }}</h1>
  <div class="flex flex-row space-x-4">
    <img
      class="w-1/2"
      src={{article.imageUrl}}
      alt=""
    />
    <p>
      {{ article.contenu }}
    </p>
  </div>
  <div class="flex flex-row justify-around my-10">
    <p>Date: {{article.dateCreation | date("d/m/Y")}}</p>
    <p>Nom: {{ article.utilisateur.nom }}</p>
  </div>
  <div class="flex flex-row justify-around my-10">
    <p class="bg-red-100 rounded p-3">{{ article.categories[0].nom }}</p>
    <p class="bg-red-100 rounded p-3">{{ article.categories[1].nom }}</p>
    <p class="bg-red-100 rounded p-3">{{ article.categories[2].nom }}</p>

  </div>
</div>

{% endblock %}
